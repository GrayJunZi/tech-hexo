/* PJAX 切换时的纯淡入淡出，不带位移，防止与内容滑动冲突 */
@keyframes fadeIns {
  from { opacity: 0; }
  to { opacity: 1; }
}
@keyframes fadeOuts {
  from { opacity: 1; }
  to { opacity: 0; }
}

.fadeIns {
  animation: fadeIns 0.3s both;
}
.fadeOuts {
  animation: fadeOuts 0.3s both;
}
