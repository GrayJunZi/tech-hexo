<% if (!index && page.comments && post.comments && theme.comment.on && theme.comment.type){ %>
    <%- partial('comments/'+theme.comment.type, {
        shortname: theme.disqus.shortname,
        url: config.url+url_for(post.path)
    }) %>
<% } %>
<!--我可能把主页index元素误删了，所以添加了页面comments元素为false时不展示评论的功能-->